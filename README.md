# Lisp Interpreter in Go

A simple LISP interpreter (basic Polish notation expression evaluation) written in Go to see how the language works.

Read-Eval-Print Loop (REPL)

To run (after cloning): `cd cmd/lisp-repl && go run .`
